<%- include('_analytics-body') %>
<%
// define
const DEFINE = (typeof define === 'undefined') ? '' : define

// metaData
const metaData = (typeof meta === 'undefined') ? '' : meta;

// SITESETTING
const SITESETTING = (typeof siteSetting === 'undefined') ? '' : siteSetting;
%>

<header class="l-header">
  <div class="l-header__inner">
    <a class="l-header__logo" href="/">
      <% if(metaData.pageName === 'top') { %>
        <h1>Quicint</h1>
      <% } %>

      <% if(metaData.pageName !== 'top') { %>
        <p>Quicint</p>
      <% } %>
    </a>
    <%- include('_nav') %>
  </div>

  <button id="hmb" class="c-hmb l-sm" aria-label="メニューボタン">
    <span></span>
  </button>
  <div id="hmb__bg" class="c-hmb-bg"></div>
</header>
